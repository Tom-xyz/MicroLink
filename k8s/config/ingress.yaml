apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-and-api-ingress
spec:
  rules:
  - https:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: microlink-web
            port:
              name: https
      - path: /shorten_url
        pathType: Prefix
        backend:
          service:
            name: microlink-api
            port:
              name: https
      - path: /*
        pathType: Prefix
        backend:
          service:
            name: microlink-api
            port:
              name: https
